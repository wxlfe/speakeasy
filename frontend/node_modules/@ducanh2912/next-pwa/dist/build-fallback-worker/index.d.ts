import type { FallbackRoutes } from "../types.js";
export declare const buildFallbackWorker: ({ id, fallbacks, destDir, minify, }: {
    id: string;
    fallbacks: FallbackRoutes;
    baseDir: string;
    destDir: string;
    minify: boolean;
    pageExtensions: string[];
    isAppDirEnabled: boolean;
}) => {
    name: string;
    precaches: (string | boolean)[];
} | undefined;
